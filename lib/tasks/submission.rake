namespace :submission do
  desc "Update new TD content in existing PT submissions where id of Trait is missing"
  task add_missing_trait_id_to_new_trait_descriptors: :environment do
    # TODO
  end
end
