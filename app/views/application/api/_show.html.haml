.action
  %span.method GET
  %span.url /api/v1/#{model.name.underscore.pluralize}/<code>:id</code>

%h4 Example
%pre
  %code.http= t("api.#{model.name.underscore}.show.example", default: :'api.default.show.example', url: "/api/v1/#{model.name.underscore.pluralize}/#{model.first.id}")

= render partial: 'application/api/try_it', locals: { url: "/api/v1/#{model.name.underscore.pluralize}/#{model.first.id}" }
