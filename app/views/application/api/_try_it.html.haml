.api-live-example
  - if user_signed_in?
    %button.btn.btn-default{type: 'button', data: { url: url } } Try it!

    .result.hidden
      .description Result of the call:
      .response
        %pre
          %code

  - else
    .alert.alert-info Sign in to try it in your browser!
