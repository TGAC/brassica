.action
  %span.method GET
  %span.url= "/api/v1/#{model.name.underscore.pluralize}"

%h4 Example
%pre
  %code.http= t("api.#{model.name.underscore}.index.example", default: :'api.default.index.example', url: "/api/v1/#{model.name.underscore.pluralize}?per_page=3")

= render partial: 'application/api/try_it', locals: { url: "/api/v1/#{model.name.underscore.pluralize}?per_page=3" }
