require 'rails_helper'

RSpec.describe TraitDescriptor do
  describe '#table_data' do
    it 'properly calculates associated trait score number' do
      pending 'Implement appropriate factories and test joins and calculations'
      fail
    end

    it 'orders trait by species name' do
      pending 'join factories and test ordering by tt.name'
      fail
    end

    it 'gets proper columns' do
      pending 'test TD columns for data tables'
      fail
    end
  end
end
